# Future Plans — Выполненные задачи (архив)

## Парсинг

### Исправление парсера Racing ✅ ВЫПОЛНЕНО
**Статус:** Выполнено (2026-07-02)

**Что было сделано:**
- ✅ Поддержка двух форматов таблиц рейсинга (2025 CC: 18 ячеек, 2026 ВС: 21 ячейка)
- ✅ Автоматическое определение формата по количеству ячеек
- ✅ Добавлены функции extractCellNumber(), parseTime(), parseSpeed()
- ✅ Создан тестовый скрипт backend/scripts/test/test-racing-formats.ts
- ✅ Созданы тестовые HTML файлы для обоих форматов
- ✅ Оба формата успешно парсятся

**Файлы:**
- backend/parsers/parse-results-racing.ts - обновлен для поддержки обоих форматов
- backend/scripts/test/test-racing-formats.ts - тестовый скрипт
- backend/scripts/test/racing-2025-test.html - тестовые данные формата 2025

---

### Парсинг рейсинга (Racing) ✅ ВЫПОЛНЕНО (основное)
**Статус:** Парсер поддерживает форматы 2025 CC (18 ячеек) и 2026 ВС (21 ячейка). Фикстуры исправлены.

**Фикстуры racing:**
- `backend/tests/fixtures/racing/2026-05-16_Complete_Results_Racing.html`
- `backend/tests/fixtures/racing/Complete_Results_2025-cc-sample.html`

(Ранее в папке `racing/` лежали BZMP-файлы с суффиксом `_B` — удалены/заменены.)

**Тестирование:** `npm run test-parser-fixtures` (v2 модульные парсеры)

---

### Фикстуры для парсеров ✅ ВЫПОЛНЕНО
**Статус:** Выполнено (2026-07).

**Сделано:**
- Реальный HTML в `backend/tests/fixtures/{coursing,bzmp,racing}/`
- `npm run download-fixtures` — загрузка с procoursing.ru
- `npm run test-parser-fixtures` — тест v2 модульных парсеров на фикстурах
- Racing-фикстуры исправлены (были ошибочно BZMP-файлы)

---

## Статистика судей

### Карточки вместо таблицы ✅ ВЫПОЛНЕНО (2026-07-07)
- `JudgeCard.tsx`, grid на `/judges`
- Таблица удалена

### Фантомный судья ✅ РЕШЕНО
**Статус:** Проблема не воспроизводится (2026-07). Ранее могла быть связана с ошибками парсинга фамилий; после исправлений парсеров и фикстур — не актуально.

---

## SPA Routing

### Сохранение позиции при перезагрузке ✅ ВЫПОЛНЕНО
**Статус:** Выполнено - сайт развернут на Cloudflare Pages с SPA routing.

**Решение:** Фронтенд развернут на Cloudflare Pages, который автоматически поддерживает SPA routing. API остаётся на Worker.

---

## UI / Мобильная адаптация

### Рекорды Донино — переименование вкладок в "Статистике" ✅ ВЫПОЛНЕНО
**Статус:** Выполнено — вкладки переименованы в единственное число.

### Рейтинг собак — год по умолчанию 2026 и корректный сброс фильтров ✅ ВЫПОЛНЕНО
**Статус:** Выполнено — год по умолчанию 2026, кнопка "Сбросить" возвращает к 2026.

### Логотип в шапке — ссылка на главную ✅ ВЫПОЛНЕНО
**Статус:** Выполнено — логотип обёрнут в `<Link to="/">` на десктопе и мобильном.

---

## Навигация

### Переименовать вкладку "Procoursing" в "Соревнования" ✅ ВЫПОЛНЕНО
**Статус:** Выполнено — текст навигационных ссылок изменён на "Соревнования" (desktop и mobile).

---

## Главная и профили Донино

### SEO на профилях Донино ✅ ВЫПОЛНЕНО (2026-07-06)

- `<SEO>` в `DoninoDogProfile.tsx` (title, description с лучшим замером / временем 350 м, keywords)
- URL в динамическом sitemap: `UNION` `speed_records` + `coursing_records` → `/donino-dog/:name/:breed`
- Пример в `docs/SEO.md`

**Sitemap:** перезагружать вручную не нужно — `api.coursing-stats.ru/sitemap.xml` генерируется при каждом запросе. После деплоя Worker роботы подхватят новые URL сами; при желании — «Проверить» / переобход в Яндекс.Вебмастер и Google Search Console.

### Фон страницы: `DogProfile` vs `DoninoDogProfile` ✅ ВЫПОЛНЕНО (2026-07-06)

У `DogProfile` убран `bg-cream-50` / `dark:bg-charcoal-900` с внешней обёртки — как у `DoninoDogProfile`: `p-4 md:p-6`, фон приложения, карточки с border.
