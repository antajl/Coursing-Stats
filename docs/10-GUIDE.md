# Guide — Справочник `/guide`

Страница **Справочник** на сайте: правила РКФ по **соревнованиям** (курсинг, бега) и **выставкам** (conformation), расшифровка протоколов, формула рейтинга CS, о проекте.

**Код:** `frontend/src/pages/Guide/`

**URL:** `/guide?tab=<id>` — раздел в query-параметре; переключение из меню **Справка** в шапке (`GUIDE_MENU_ITEMS` в `navLinks.ts`).

---

## Вкладки

| id | Подпись в UI | Файл | Содержание |
|----|--------------|------|------------|
| `titles` | Соревнования | `components/TitlesTab.tsx` | CACL, CACIL, титулы ЧР/ПК/ГЧ РК, статусные соревнования, особенности курсинга |
| `shows` | Выставки | `components/ShowsTab.tsx` | CAC, CACIB, BOB, BIS, ранги выставок, чемпионские титулы по красоте |
| `protocol` | Протоколы | `components/ProtocolTab.tsx` | колонки протокола курсинга и бегов, квалификация ВС |
| `rating` | Рейтинг | `components/RatingTab.tsx` | медали vs очки, индекс CS v1 (кратко) |
| `site` | О сайте | `components/SiteTab.tsx` | что такое Coursing Stats, контакты, источники |

Переключение: выпадающее меню **Справка** в `NavDesktop` / `NavMobile` (как Соревнования / Выставки / Донино).

**По умолчанию:** `tab=titles` (Соревнования).

---

## Единая структура вкладок «Соревнования» и «Выставки»

Обе дисциплинные вкладки построены **одинаково по порядку блоков** — пользователь читает сверху вниз: общая карта → связь с сайтом → детали → справочник аббревиатур.

| # | Блок | Соревнования | Выставки |
|---|------|--------------|----------|
| 1 | Официальные источники | PDF/положения РКФ по курсингу и бегам | Положение о сертификатных выставках, титулах, FCI EXP-REG |
| 2 | **Иерархия** (4 уровня) | Крутые → сертификаты → титулы дня → собирательные | То же для conformation |
| 3 | **На этом сайте** | Раздел «Соревнования», два рейтинга | Раздел «Выставки», бейджи по категориям |
| 4 | **Сертификаты** | CACL / CACIL / CACLBr | CACIB / CAC / JCAC / VCAC |
| 5 | **Награды на одном событии** | карточки EVENT_TITLES | карточки + нумерованный приоритет |
| 6 | **Ранги: кто что получает** | таблица STATUS_EVENT_RULES | таблица SHOW_RANKS |
| 7 | **Кумулятивные титулы** | НЧ РК, ПЧ РК, ГЧР РК, ГЧРКФ РК | C.I.B., Чемпион России … |
| 8 | **Особенности дисциплины** | COURSING_REQUIREMENTS | SHOW_FEATURE_NOTES |
| 9 | **Сокращения** | ABBREVIATIONS | SHOW_ABBREVIATIONS |

**Категории бейджей:** `prestige` / `certificate` / `diploma` / `cumulative` — см. `frontend/src/lib/awardCategories.ts`, выставочные ключи в `SHOW_AWARD_CATEGORY`.

**Иерархия наград выставок (один день):** BIS → BIG → BOB/ЛПП → BOS → ЛБ/ЛЩ/ЛЮ/ЛВ → CACIB → CAC/JCAC/VCAC → дипломы дня → CW/резервы/СС.  
**Рейтинг выставок:** одна колонка; `rank_score` из `show-award-ranking.ts`; на главной метрика топа — `formatShowRankingReason` (счётчики титулов). По умолчанию в UI — текущий сезон. Локально протокол `/shows/exhibition/:id` — главный ринг (вкладки), каталог пород, клички → профиль; шапка — пилюли + мета-строка клуб/ранг/тип.

**Кумулятивные титулы выставок (оформление в РКФ):** C.I.B. → Чемпион России → Юный чемпион → Чемпион РКФ → ветераны → КЧК.

**Данные / PDF:** канон — [`SHOWS.md`](SHOWS.md) (парсер type1, годы с 2026 вниз, лимит 25 MB).

---

## Файлы данных и констант

### Соревнования — `constants.ts`

| Константа | Назначение |
|-----------|------------|
| `OFFICIAL_SOURCES` | Ссылки на PDF РКФ; `supplementary: true` — вспомогательные (презентация Донино) |
| `COURSING_CERTIFICATES` | Уровни CACL / CACIL / CACLBr |
| `EVENT_TITLES` | Титулы, присваиваемые на мероприятии (ЧР РК, ПКР РК …) |
| `STATUS_EVENT_RULES` | Кто что получает на статусных (без выдуманных «зазоров» от 1-го места) |
| `CUMULATIVE_TITLES` | НЧ РК, ПЧ РК, ГЧР РК, ГЧРКФ РК |
| `COURSING_CACL_QUALIFICATION` | Норма CACL (≥⅔ max баллов на статусных) |
| `COURSING_REQUIREMENTS` | Особенности курсинга (мин. участников, второй круг …) |
| `NATIONAL_CHAMPION_VARIANTS`, `CH_RUSSIA_VARIANTS` | Варианты оформления титулов |
| `ABBREVIATIONS` | Словарь для `AbbrTag` на вкладке «Соревнования» |

### Выставки — `showConstants.ts`

| Константа | Назначение |
|-----------|------------|
| `SHOW_OFFICIAL_SOURCES` | Положение о сертификатных выставках (ред. 18.12.2024, с 01.01.2025), титулы РКФ, FCI |
| `SHOW_CERTIFICATES` | CACIB / CAC / JCAC |
| `SHOW_EVENT_TITLES` | Карточки BIS, BOB, ЧРКФ, ЧФ |
| `SHOW_EVENT_AWARDS_PRIORITY` | Полный приоритет наград одного дня (rank 1–9) |
| `SHOW_RANKS` | Ранги выставок РКФ (CACIB FCI … монопородные) |
| `SHOW_CHAMPIONSHIP_TITLES` | Кумулятивные титулы по красоте |
| `SHOW_CLASSES`, `SHOW_GRADES` | Классы и оценки (используются в `SHOW_FEATURE_NOTES`) |
| `SHOW_FEATURE_NOTES` | Блок «Особенности выставок» |
| `SHOW_ABBREVIATIONS`, `SHOW_ABBR_LOOKUP` | Словарь для вкладки «Выставки» |

---

## Общие UI-компоненты

**Файл:** `components/GuideUi.tsx`

| Компонент | Назначение |
|-----------|------------|
| `AbbrTag` | Аббревиатура с tooltip (`ABBREVIATIONS` или переданный `title`) |
| `SectionCard` | Карточка раздела с заголовком h2 |
| `InfoCallout` | Выделенная заметка (camel border) |
| `OfficialSourcesList` | Список ссылок на документы РКФ |
| `TitleHierarchySection` | Вложенная иерархия (3 tier: root / mid / leaf) |
| `CertificateLevelsGrid` | Сетка уровней сертификатов |
| `EventTitlesGrid` | Карточки наград на одном событии |
| `PriorityAwardsList` | Нумерованный список приоритета (выставки) |
| `CumulativeTitlesGrid` | Сетка кумулятивных титулов |
| `FeatureNotesGrid` | Сетка «особенностей» дисциплины |
| `AbbreviationsTable` | Таблица сокращений |
| `TitleBadge`, `RefTag`, `ExternalHref` | Бейджи титулов, ссылки на пункты положений, внешние URL |

---

## Источники и правила редактирования

### Соревнования (курсинг / бега)

1. **Нормы РКФ** — только из официальных документов (правила курсинга, положение о титулах). При сомнении — сверка с PDF, не домыслы.
2. **CACL на 2-м месте** — положение п. 3.4.2, правила курсинга п. 4.3.4 (≥⅔ max).
3. **Нет официального правила** «зазора в очках от 1-го места» — из UI убрано намеренно.
4. **CC и ВС** — одна колонка квалификации, разные подписи в HTML протоколов (2025 vs 2026).
5. **Два рейтинга на сайте** (медали vs очки) — вкладка **«Рейтинг»**, не «Соревнования». Формула CS v1 — `RatingTab.tsx`, код `backend/lib/rating/coursing-rating-score.ts`.

### Выставки (conformation)

1. **Положение о сертификатных выставках РКФ** (ред. 18.12.2024) — ранги, классы, CAC/CACIB, BOB, BIG, BIS.
2. **Положение о титулах РКФ** — оформление Чемпиона России, C.I.B., юных и ветеранов **по красоте** (не путать с рабочими титулами курсинга).
3. **CAC ≠ титул «Чемпион»** — сертификат на выставке; титул оформляется в РКФ по набору.
4. **speed_records / coursing_records** — не смешивать с выставками (отдельный раздел Донино).
5. При правке приоритетов — синхронизировать `SHOW_EVENT_AWARDS_PRIORITY` и порядок бейджей в `ShowDogCard`.
6. **Названия пород на сайте** — sentence case + обиходные подписи (левретка / малинуа…); тип шерсти не угадываем (выжла/НО без типа → «тип не указан»). Канон и фильтры: [`03-DATA.md`](03-DATA.md) → «Канон и отображение»; чистка «порода+судья» в индексах: [`SHOWS.md`](SHOWS.md).

### Общее

- Вкладки **«Соревнования»** и **«Выставки»** — **разные дисциплины**; перекрёстные ссылки в intro-блоках.
- Не объединять рейтинги мест и очков; не делить `total_score` на число судей.
- Тексты справочника — **не** источник правды для парсеров; парсеры ориентируются на протоколы и PDF РКФ.

---

## SEO

Маршрут `/guide` в sitemap — генерируется в CI (`build-derived-indexes` → `sitemap.xml`).

Query-параметры вкладок (`?tab=shows` и т.д.) — клиентская навигация; основной URL в sitemap — `/guide`.

---

## Связь с другими разделами сайта

| Раздел сайта | Справка |
|--------------|---------|
| Соревнования → Рейтинг, Судьи | `/guide?tab=titles`, `/guide?tab=rating`, `/guide?tab=protocol` |
| Выставки → Рейтинг, Судьи (календарь — только local DEV) | `/guide?tab=shows` |
| История выставок в профиле → lc.rkfshow.ru (на проде) | `/guide?tab=shows`; данные: [`SHOWS.md`](SHOWS.md) |
| Подсказка ⓘ у «очки» | `/guide?tab=rating` |
| Раздел выставок (данные, парсинг) | [`SHOWS.md`](SHOWS.md) |

---

## Тесты

- E2E: `e2e/guide.spec.ts` — загрузка, переключение вкладок.
- При добавлении раздела — обновить `GUIDE_MENU_ITEMS` в `navLinks.ts`, `GUIDE_SECTIONS` в `Guide/index.tsx`, этот файл и e2e.

---

## См. также

- [`06-DESIGN-SYSTEM.md`](06-DESIGN-SYSTEM.md) — паттерны UI, подсказки рейтинга
- [`04-FRONTEND.md`](04-FRONTEND.md) — карта `frontend/src/pages/Guide/`
- [`SHOWS.md`](SHOWS.md) — данные и парсинг выставок (не дублировать в Guide тексты про lc.rkfshow.ru)
