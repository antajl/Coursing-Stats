# SPA: все клиентские маршруты → index.html.
# НЕ добавлять /404.html в public — иначе Cloudflare Pages отключает SPA-режим
# и /competitions|/shows|… отдают 404 (см. docs/16-TROUBLESHOOTING.md).
# НЕ ставить rewrite /assets/* → 404: на Pages status 404 в _redirects не поддерживается
# и может сломать разбор файла.
/*    /index.html   200
